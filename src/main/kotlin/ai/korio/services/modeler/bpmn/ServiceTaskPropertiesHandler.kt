package ai.korio.services.modeler.bpmn

class ServiceTaskPropertiesHandler {

    /**
     * 1. Create a "type" field (or subclass Camunda Service Task) for a PUBLISH SERVICE.  It should be attached to a Data Service, which
     * will likely be a BPMN process.
     * */
}